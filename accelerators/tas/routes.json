{
  "routes": [
    {
      "title": "{{apiGroup2}} login",
      "description": "SSO enabled path used to login for {{apiGroup2}}",
      "path": "/{{apiGroup2}}/login",
      "method": "GET",
      "sso-enabled": true,
      "filters": ["RedirectTo=302, /{{apiGroup1}}"],
      "order": 0
    },
    {
      "title": "{{apiGroup1}} open home page",
      "description": "{{apiGroup1}} home page that doesn't require SSO login",
      "path": "/{{apiGroup1}}/",
      "method": "GET",
      "order": 1000,
      "filters": [ "CircuitBreaker=frontendCb,forward:/fallback" ]
    }
  ]
}
