{
  "routes": [
    {
      "title": "onlinestore login link",
      "description": "SSO enabled path used by the login button",
      "path": "/store/login",
      "method": "GET",
      "sso-enabled": true,
      "filters": ["RedirectTo=302, /store"],
      "order": 0
    },
    {
      "title": "onlinestore home page",
      "description": "onlinestore home page that doesn't require SSO login",
      "path": "/store/**",
      "method": "GET",
      "order": 1000,
      "filters": [ "CircuitBreaker=frontendCb,forward:/fallback" ]
    }
  ]
}
